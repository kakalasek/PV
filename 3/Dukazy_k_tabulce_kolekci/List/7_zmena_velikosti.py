"""
Lists in python are mutable and you can freely expand them.
"""
very_expandable_list = ["Thicc", "List"]
print(f"The size of the before: {len(very_expandable_list)}")
very_expandable_list += ["Big"]
print(f"The size of the list after: {len(very_expandable_list)}")