"""
We have several methods to get the number of elements in a tuple. The easiest is to use len(), but we can also write somehting ourselves
"""
t = (1, "hell", (2, 4))
print(f"Number of elements using len(): {len(t)}")